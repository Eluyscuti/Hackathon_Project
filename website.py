#website builder
from flask import Flask
from PIL import image

app = flask(fire)
@app.route(‘/’)
def home():
return 'Note: this content dependent on the lack of change of current climate policies.'
image = Image.fromarray(pixels)

if fire ==’main’:
    app.run(debug=True)